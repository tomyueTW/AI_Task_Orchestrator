REDIS_HOST=localhost
REDIS_PORT=6379
WORKER_CONCURRENCY=3
MAX_CONCURRENCY_PER_USER=1
# BACKPRESSURE_THRESHOLD=300  # Optional: defaults to WORKER_CONCURRENCY * 100
# IDEMPOTENCY_TTL_SECONDS=86400  # Optional: defaults to 24 hours
# TASK_FAILURE_RATE=0  # Optional: 0-1 for simulating failures (testing only)
